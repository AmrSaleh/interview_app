
<script>
  $(document).ready(function() {
    
     var format = 'yy-mm-dd';
    $("#datepicker").datepicker({dateFormat: format});
  }); 
</script>

<%= form_for(@date_request, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @date_request.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@date_request.errors.count, "error") %> prohibited this date_request from being saved:</h4>

      <ul>
      <% @date_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <!-- <%= f.label :interview_id, class: "col-sm-2 control-label" %> -->
    <!-- <div class="col-sm-10"> -->
      <!-- <%= f.text_field :interview_id, class: "form-control" %> -->
       <!-- <%= f.collection_select :interview_id,Interview.all,:id,:id_with_name ,{},{:multiple => false, class: "form-control"} %>  -->
    <!-- </div> -->

<%= f.hidden_field :interview_id, value: params[:inter_id]%>

  </div>
  <div class="form-group">
    <%= f.label :requested_date, class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <!-- <%= f.text_field :requested_date, class: "form-control" %> -->
      <%= f.text_field :requested_date,id: 'datepicker',  class: "form-control" %>
    </div>

   
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
